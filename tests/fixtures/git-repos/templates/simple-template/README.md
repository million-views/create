# Simple Template Fixture

This is the smallest possible template fixture used for Git integration tests. It contains a single placeholder-aware file so cache and resolver tests can verify discovery logic without depending on large fixtures.
