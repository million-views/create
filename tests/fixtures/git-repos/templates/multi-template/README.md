# Multi-Template Registry Fixture

This fixture mimics a registry repository that exposes multiple templates under different directories (`react-app/` and `vue-app/`). Tests use it to validate registry alias resolution and discovery of templates inside cloned Git repositories.
