# {{projectName}}

A production-ready Express.js REST API server built with modern JavaScript features.

## Features

{{#if (includes features 'auth')}}- 🔐 **Authentication**: {{#if (eq auth 'jwt')}}JWT-based{{/if}}{{#if (eq auth 'passport')}}Passport.js{{/if}} authentication system{{/if}}
{{#if (includes features 'database')}}- 🗄️ **Database**: {{#if (eq database 'postgresql')}}PostgreSQL{{/if}}{{#if (eq database 'mysql')}}MySQL{{/if}}{{#if (eq database 'sqlite')}}SQLite{{/if}}{{#if (eq database 'mongodb')}}MongoDB{{/if}} integration{{/if}}
{{#if (includes features 'cors')}}- 🌐 **CORS**: Cross-Origin Resource Sharing enabled{{/if}}
{{#if (includes features 'rate-limiting')}}- 🛡️ **Rate Limiting**: Request rate limiting to prevent abuse{{/if}}
{{#if (includes features 'logging')}}- 📝 **Logging**: Structured logging with Winston{{/if}}
{{#if (includes features 'validation')}}- ✅ **Validation**: Request validation with Joi{{/if}}
{{#if (includes features 'docs')}}- 📚 **Documentation**: OpenAPI/Swagger API documentation{{/if}}
- 🚀 **Production Ready**: Security headers, error handling, health checks
- 🧪 **Testing**: Comprehensive test suite with Node.js test runner
- 📦 **ESM**: Modern ES modules throughout

## Quick Start

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Configure environment:**
   ```bash
   cp .env.example .env
   # Edit .env with your configuration
   ```

3. **Start development server:**
   ```bash
   npm run dev
   ```

4. **Run tests:**
   ```bash
   npm test
   ```

## API Endpoints

- `GET /health` - Health check endpoint
- `GET /api/v1` - API information{{#if (includes features 'docs')}}
- `GET /api/docs` - API documentation{{/if}}

## Project Structure

```
src/
├── server.js          # Main server file
├── middleware/        # Custom middleware
├── routes/           # API routes
├── models/           # Data models
├── controllers/      # Route controllers
├── services/         # Business logic
├── utils/            # Utility functions
└── config/           # Configuration files

test/
├── unit/             # Unit tests
├── integration/      # Integration tests
└── fixtures/         # Test fixtures
```

## Deployment

This API is configured for deployment on:
- **{{deployment_target}}** (primary target)
- Other supported platforms: Vercel, Railway, Render, Fly.io, Heroku

## Development

### Available Scripts

- `npm start` - Start production server
- `npm run dev` - Start development server with hot reload
- `npm test` - Run test suite
- `npm run test:watch` - Run tests in watch mode
- `npm run lint` - Lint code
- `npm run lint:fix` - Fix linting issues

### Environment Variables

See `.env.example` for all available configuration options.

## Contributing

1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Add tests for new functionality
5. Ensure all tests pass
6. Submit a pull request

## License

MIT License - see LICENSE file for details.