# Nested Subpath Fixture

This repository layout exercises subpath resolution logic. The actual template lives under `templates/starter/`, allowing tests to verify `user/repo/path/to/template` parsing as well as branch hash handling.
