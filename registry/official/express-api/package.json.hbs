{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "Express.js API Server",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "echo 'Build step - add your build process here'",
    "test": "node --test test/**/*.test.js",
    "test:watch": "node --test --watch test/**/*.test.js",
    "lint": "eslint src/**/*.js test/**/*.js",
    "lint:fix": "eslint src/**/*.js test/**/*.js --fix"
  },
  "keywords": ["api", "express", "nodejs", "rest"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.3.0"{{#if (includes features 'auth')}},
    {{#if (eq auth 'jwt')}}
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3"{{/if}}
    {{#if (eq auth 'passport')}}
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.1"{{/if}}{{/if}}{{#if (includes features 'database')}},
    {{#if (eq database 'postgresql')}}
    "pg": "^8.11.0",
    "pg-hstore": "^2.3.4"{{/if}}
    {{#if (eq database 'mysql')}}
    "mysql2": "^3.6.0"{{/if}}
    {{#if (eq database 'sqlite')}}
    "sqlite3": "^5.1.0"{{/if}}
    {{#if (eq database 'mongodb')}}
    "mongodb": "^5.7.0"{{/if}}{{/if}}{{#if (includes features 'rate-limiting')}},
    "express-rate-limit": "^7.0.0"{{/if}}{{#if (includes features 'validation')}},
    "joi": "^17.9.0"{{/if}}{{#if (includes features 'docs')}},
    "swagger-jsdoc": "^6.2.0",
    "swagger-ui-express": "^5.0.0"{{/if}}
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "eslint": "^8.50.0",
    "@eslint/js": "^0.3.0"
  }
}